# Network App releases

Public signed APK releases and the updater manifest for Network App.

- `version.json` is generated by the local `release.ps1` workflow.
- Release APKs must use the same protected signing key for the lifetime of the app.
- No network/contact data or credentials belong in this repository.